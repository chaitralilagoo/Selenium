package automation;
import org.openqa.selenium.Alert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class PopUps
{
	//Hidden Division----shows property,functionality----eg. facebook sign up (Create new account)---can inspect this popup
	//Alert----warning---(do you want to continue etc)---can't access main page---can't inspect this popup
		//to handle this--use Alert interface--
		//to switch the focus of selenium to popup----- switchTo() used
		// methods of Alert interface
		// Accept()----ok
		//Dismiss()---cancel
		//getText()---
		//sendKeys()--

}
